#include<stdio.h>
void main()
{
 long int num,i,count,sumprime=0;

for(num=2;num<2000000;num++)
{
  count=0;
   for(i=2;i<num;i++)
   {
     if(num%i==0)
     count++;
   }
if(count==0)
sumprime+=num;
}
printf("%ld",sumprime);
}
