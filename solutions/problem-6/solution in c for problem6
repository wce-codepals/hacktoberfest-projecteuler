#include <stdio.h>

int main()
{
	int n=100;
	int i,a;
	i=0;
	long long int sum=0,squareofsum=0;
	for(i=0;i<=n;i++)
	{
		sum= sum+i;
		squareofsum=squareofsum+i*i;
	}
	sum=sum*sum;
	a = sum-squareofsum;
	printf("the difference between sqaure of sum and  sum of squares is %d\n", a);
	return 0;
	
}
