#include<stdio.h>
void main()
{
  int a,b,c,i,j,k;

for(a=1;a<1000;a++)
{
   for(b=1;b<1000;b++)
   {
    for(c=1;c<1000;c++)
    {
     if( (a+b+c)==1000  && (a*a+b*b==c*c))
       {
        printf("%d\n",a*b*c);
       }
    }
   }
}
}
        
